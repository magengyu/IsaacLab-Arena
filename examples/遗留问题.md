问题1：
示例的遥操作是用XR控制机器人全身运动，或者仅上半身运动。与我们的机械臂模型不同，没有验证能否正常工作。
可以先在已有单机械臂env基础上验证。


问题2：
在example04中，尝试把相机图像加入到观测数据，但是发现enable_camera与viz kit的配置冲突，导致渲染图像时不能开启ov界面


问题3：
需要把场景分为多个独立任务，每个任务都有自己的观测数据和目标。
任务一，把桌子上箱子中的三个部件顺序组装起来。两个pick-and-place任务。只保留桌子和箱子，不需要传送带。
任务二，把箱子从传送带上搬到桌子上。之保留一段传送带。需要自定义任务成功和失败的判断标准。

